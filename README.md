# Remove paths recursively using [@actions/io](https://github.com/actions/toolkit/tree/master/packages/io)

You can now consume the action by referencing the main branch

```yaml
uses: ivorpad/remove-rf@main
with:
  path: "some/path"
```